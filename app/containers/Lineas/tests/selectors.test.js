// import { fromJS } from 'immutable';
// import { makeSelectLineasDomain } from '../selectors';

// const selector = makeSelectLineasDomain();

describe('makeSelectLineasDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
