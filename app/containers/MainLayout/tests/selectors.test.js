// import { fromJS } from 'immutable';
// import { makeSelectMainLayoutDomain } from '../selectors';

// const selector = makeSelectMainLayoutDomain();

describe('makeSelectMainLayoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
